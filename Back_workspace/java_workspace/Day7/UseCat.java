//현실의 사물을 표현하기 위한 클래스가 아니라, 그냥 실행부를 두기 위한 클래스임. 
//따라서 이 클래스는 재활용성이 없다. 

class UseCat 
{
	public static void main(String[] args) 
	{
		new Cat();//Cat 클래스의 생성자가 눈에 보이지않더라도
		// 연산자 뒤에서 에러가 나지 않는 이유? 컴파일러에 의한 디폿ㄹ트 생성자의 존재 때문이다.
		//생성자도 메서드이다. 따라서 메서드 오버로딩의 원칙이 당연히 적용된
		pulbic Cat(){
			{
	}
		public cat(htre
}
